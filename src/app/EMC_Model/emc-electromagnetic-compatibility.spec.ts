import { EmcElectromagneticCompatibility } from './emc-electromagnetic-compatibility';

describe('EmcElectromagneticCompatibility', () => {
  it('should create an instance', () => {
    expect(new EmcElectromagneticCompatibility()).toBeTruthy();
  });
});
