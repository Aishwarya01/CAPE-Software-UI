import { AirterminationConstants } from './airtermination-constants';

describe('AirterminationConstants', () => {
  it('should create an instance', () => {
    expect(new AirterminationConstants()).toBeTruthy();
  });
});
