import { UpdatePasswordInspector } from './update-password-inspector';

describe('UpdatePasswordInspector', () => {
  it('should create an instance', () => {
    expect(new UpdatePasswordInspector()).toBeTruthy();
  });
});
