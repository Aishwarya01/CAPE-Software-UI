import { ObservationInspection } from './observation-inspection';

describe('ObservationInspection', () => {
  it('should create an instance', () => {
    expect(new ObservationInspection()).toBeTruthy();
  });
});
