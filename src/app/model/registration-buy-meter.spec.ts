import { RegistrationBuyMeter } from './registration-buy-meter';

describe('RegistrationBuyMeter', () => {
  it('should create an instance', () => {
    expect(new RegistrationBuyMeter()).toBeTruthy();
  });
});
